if not ( RES_DISTRIB == 2 ) then Error("Please Install Resource Distribution 2 Addon.'" ) return end

TOOL.Category			= "(Life Support)"
TOOL.Name				= "#Conflict Devices"

TOOL.DeviceName			= "Conflict Device"
TOOL.DeviceNamePlural	= "Conflict Devices"
TOOL.ClassName			= "receptacles"

TOOL.DevSelect			= true
TOOL.CCVar_type			= "conflict_target_select"
TOOL.CCVar_sub_type		= "small"
TOOL.CCVar_model		= "models/props_vents/vent_large_grill001.mdl"

TOOL.Limited			= true
TOOL.LimitName			= "conflictdevice"
TOOL.Limit				= 30

if (CLIENT and GetConVarNumber("RD_UseLSTab") == 1) then TOOL.Tab = "Life Support" end

RD2ToolSetup.SetLang("Life Support Storage Devices","Create Conflict Devices attached to any surface.","Left-Click: Spawn a Device.  Reload: Repair Device.")

TOOL.Devices = {
		
	conflict_target_select = {
			name = "Targeting Computer",
			type = "conflict_target_select",
			devices = {
				small = {
					name = "test",
					model = "models/props_vents/vent_large_grill001.mdl",
					maxhealth = 100,
					mass = 10,
						}
					}
		}
}	

--local weaponmodels = {}
--table.insert(weaponmodels, {'Weapon Indicator'  , 'models/props_c17/clock01.mdl'              , 'conflict_status_indicator' })
--table.insert(weaponmodels, {'Targeting Computer', 'models/props_vents/vent_large_grill001.mdl', 'conflict_target_select'    })

RD2_ToolRegister( TOOL, weaponmodels, nil, "conflict_devices", 10,conflict_devices )
